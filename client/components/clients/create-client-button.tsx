"use client";

import { Button } from "../ui/button";

export const CreateClientButton = () => {
  return (
    <Button variant="default" className="ml-auto" onClick={() => null}>
      Kunde anlegen
    </Button>
  );
};
